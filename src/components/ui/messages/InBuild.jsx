function InBuild({ title }) {
    return (
        <section className="hero-container">
            <h2 className="hero-container_title">
                { title }
            </h2>
            <p className="hero-container_description">
                En construction
            </p>
        </section>
    )
}

export default InBuild;