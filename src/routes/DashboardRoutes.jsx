import { Route, Routes } from "react-router";

function DashboardRoutes(params) {
    return (
        <Routes>
            <Route index element="" />
        </Routes>
    )
}

export default DashboardRoutes;